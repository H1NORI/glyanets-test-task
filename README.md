COMANDS I USED:
```
composer create-project drupal/recommended-project glyanets-test-task
vendor/bin/drush en my_first_module
```

COMANDS USED TO RUN SERVER:
```
php -S localhost:8000 -t web
```

COMANDS USED TO SEE MODULES LIST:
```
vendor/bin/drush pml
```

COMANDS USED TO USE AFTER ANY UPDATE IN ROUTES:
```
vendor/bin/drush cr
```


added .gitignore but I need to rechek if it works correctly
